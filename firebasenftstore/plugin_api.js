/**
 * plugin_api.js
 * API for extending the extension with custom encryption or challenge plugins.
 */

export function registerPlugin(plugin) {
  // plugin = { encrypt, decrypt, onChallenge, etc. }
}
